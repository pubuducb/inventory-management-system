# Overview of the files in the folder `internal/server/`

## API Wiring `server.go`

Sets up the HTTP server by wiring Repositories and Handlers, configuring Routes, and applying server settings such as the `port`.
